# RSC without a Framework

Understanding how RSCs are implemented without a framework

`npm run dev:client` => To run the client
`npm run dev:server` => To run the server

Pretty simple Fastify app serving webpack bundle
